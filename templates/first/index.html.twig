{% extends 'base.html.twig' %}

{% block title %}Hello FirstController!{% endblock %}

{% block body %}
    <div class="card mt-2 text-center">
        <div class="card-body">
        <h1><strong>Dit wil ik geven</strong></h1>
            Ketting (Die heb je al als het goed is :) )<br>
            En een dagje Amsterdam op mijn kosten leuke dingen doen met mij 😅
        </div>
        <div class="card-footer">
            ( Sorry voor al die confettie vond het veel te leuk om niet te doen ) <br>
            <a href="{{ path('result_new') }}" class="btn btn-success">Verder</a>
        </div>
    </div>
    </div>

    <script>
        var duration = 15 * 1000;
        var animationEnd = Date.now() + duration;
        var defaults = { startVelocity: 30, spread: 360, ticks: 60, zIndex: 0 };

        function randomInRange(min, max) {
            return Math.random() * (max - min) + min;
        }

        var interval = setInterval(function() {
            var timeLeft = animationEnd - Date.now();

            if (timeLeft <= 0) {
                return clearInterval(interval);
            }

            var particleCount = 50 * (timeLeft / duration);
            // since particles fall down, start a bit higher than random
            confetti(Object.assign({}, defaults, { particleCount, origin: { x: randomInRange(0.1, 0.3), y: Math.random() - 0.2 } }));
            confetti(Object.assign({}, defaults, { particleCount, origin: { x: randomInRange(0.7, 0.9), y: Math.random() - 0.2 } }));
        }, 250);
    </script>

{% endblock %}
